services:
  - type: web
    name: mcp-demo
    runtime: node
    plan: starter
    region: oregon
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    domains:
      - mcp.demo.cjav.dev
