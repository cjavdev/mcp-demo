services:
  - type: web
    name: mcp-demo
    plan: starter
    region: oregon
    buildCommand: bun install
    startCommand: bun start
    envVars:
      - key: NODE_ENV
        value: production
    domains:
      - mcp.demo.cjav.dev
